# Active causal diagnosis v6 contract rejection

Status: invalid and terminal. The one authorized non-empirical cache diagnostic
was consumed on 2026-08-08 and returned `contract-rejection` (H0). Under the
accepted decision table, this result does not authorize the predetermined
cache-walker recovery and ends the extended vocabulary research program before
Phase 3.

## Fixed provenance

- governing program: `e6dd2fef97a80e24ceae5b2bb531d09ae310c0aa`;
- accepted diagnostic plan P6:
  `4881641ade773a07280990f19e89277c7e12c056`;
- diagnostic implementation X6:
  `8c476437e38ab5cf60119847dcd6204ed5cf46e0`;
- pretraining commit E3: `89a9221e97dd17d6ba220a22a12d4c0328417ffb`;
- evidence identity R3: `7482d1b9712f49cb988623a87ec8bb1c34667a26`;
- failed-v5 predecessor receipt SHA-256:
  `2e3c67fe774b288d72ea8672f9b07a063533a377a1394ba5de082f9c316a6577`;
- diagnostic receipt SHA-256:
  `b8be7907ab6de6bc4589c2d95d126a03bc35d095d1e3105bbb9bb0f8664d0da1`;
- diagnostic operator SHA-256:
  `21b208654a2c3cb2404bff356c6742b4ff18a72743b863c2571e6d5207689465`.

The receipt is stored outside the worktree at
`.git/nous-attempts/active-causal-diagnosis-v6-cache-diagnostic.json`. It is
canonical, has state `completed`, records exactly one worker start, and records
the normalized result `contract-rejection`. It contains no training or bundle
digest field and no path, filename, count, size, mode, link count, device,
inode, or private metadata digest.

## Observed result

The separately built, clean-X6 operator was invoked exactly once from the
physical repository root under the accepted protected environment. Its exact
human-visible output was:

```text
diagnostic: contract-rejection
```

The fixed E3 worker completed the development-derived workload and reached the
expected deliberate training-digest rejection without publishing replay
outputs. Within one stable metadata bracket, both reference passes, both
current-walker passes, and both candidate-walker passes rejected the resulting
diagnostic tree. The outer environment and root identities remained stable.

The result deliberately does not disclose which frozen cache predicate was
violated. It says nothing about the deleted v5 tree and does not prove that the
historical v5 rejection was correct. It does prove that the predetermined
`unix.ReadDirent` candidate does not turn this fixed diagnostic workload into
an accepted tree while preserving the frozen contract, so H1 was not reached.

## Consequence

The diagnostic created no replay, validation, locked, or empirical result
record. Its temporary build and execution worktrees were removed, the main
worktree remained clean, and no diagnostic process remained running.

The accepted decision table classifies H0 as a valid audit rejection of only
the diagnostic tree. It leaves Phase 2 `invalid`, authorizes no recovery
amendment, retry, looser cache contract, or further diagnostic version, and
terminates phases 3 through 10 without executing them.
